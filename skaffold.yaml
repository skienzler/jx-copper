apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/skienzler/jx-copper:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/skienzler/jx-copper:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-copper
        chartPath: charts/jx-copper
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/skienzler/jx-copper'
          image.tag: '{{.DIGEST_HEX}}'
